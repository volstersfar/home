version: '2'
volumes:
    app-data:
services:
  homeassistant:
    build: ./homeassistant
    container_name: homeassistant
    restart: always
    network_mode: host
    volumes:
      - app-data:/config
